{
  "name": "hugo-tailwind-blog",
  "version": "1.0.0",
  "description": "Hugo blog template with Tailwind CSS v4 and typography",
  "scripts": {
    "dev": "hugo server --buildDrafts --buildFuture",
    "build": "hugo --minify",
    "clean": "rm -rf public resources hugo_stats.json"
  },
  "keywords": ["hugo", "blog", "tailwind", "typography"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^4.1.16"
  }
}
