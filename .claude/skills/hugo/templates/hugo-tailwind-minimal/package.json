{
  "name": "hugo-tailwind-minimal",
  "version": "1.0.0",
  "description": "Minimal Hugo site with Tailwind CSS v4",
  "scripts": {
    "dev": "hugo server --buildDrafts --buildFuture",
    "build": "hugo --minify",
    "clean": "rm -rf public resources hugo_stats.json"
  },
  "keywords": ["hugo", "tailwind", "static-site"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^4.1.16"
  }
}
