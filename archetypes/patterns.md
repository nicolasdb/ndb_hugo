---
title: "{{ replace .File.ContentBaseName "-" " " | title }}" # auto-generated from filename; edit to final title
date: {{ .Date }} # publication date; use current or past date (future dates won't publish)
draft: true # set to false when publishing; true hides from listing
description: "One sentence description of this skill pattern." # 1–2 sentence elevator pitch (no markdown)
tags: [] # category tags (governance, electronics, infrastructure, etc.)
confidence: 70          # 0–100; see thresholds: 85+ strong, 70–84 solid, <70 emerging
blockCount: 0           # number of evidence blocks backing this pattern (count from content)
timespan: "YYYY – present" # duration of demonstrated skill; required format: replace YYYY with start year (e.g., "2019 – present")
trajectory: stable      # rising | stable | converging (trajectory of the skill over time)
featuredOnHomepage: false # set true to pin this pattern to the homepage patterns section
---

<!-- Write 1–3 paragraphs describing this skill pattern. Be honest and specific. -->
